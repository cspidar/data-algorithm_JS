//

function sol(p1, p2) {
  const limit = Number(p1);
  const pick = Number(p2);

  function DFS(L) {
    if (L === 4) {
    } else {
      let tmp = '';
    }
  }

  DFS(0);

  return res[0];
}

const in1 = 3;
const in2 = 2;

console.log(sol(in1, in2));
// !console.table(sol(in1, in2));
