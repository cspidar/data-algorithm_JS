//
function sol(str1, str2) {
  //
  //
  const sarr = str1.split('');

  while (str1.length == 0) {}

  const ans = 'YES';

  return ans;
}

const in1 = '(A(BC)D)EF(G(H)(IJ)K)LM(N)';
const in2 = 'abc';

console.log(sol(in1, in2));
!console.table(sol(in1, in2));
